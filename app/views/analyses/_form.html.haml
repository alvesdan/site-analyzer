= form_for analysis, remote: true do |f|
  .form-group
    = f.text_field :url,
    class: 'form-control',
    placeholder: 'What site do you want to analyze?',
    autocomplete: 'off'
    = field_error(f, analysis, :url)
